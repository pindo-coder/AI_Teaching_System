# 优化日志

本文件记录面向测试与迭代的功能优化。正式版本发布时，再同步整理至根目录 `CHANGELOG.md`。

## 2026-07-13

### 账号、角色与课堂互动

- 注册页增加学生、教师角色选择；学生填写学号，教师填写工号。
- 学号/工号进行格式与唯一性校验，并在用户资料中保存身份编号。
- 教师和管理员可发布课堂互动；学生可围绕互动主题提交观点。

### 教材与课程学习

- 教材 PDF 导入后支持自动识别章节并创建对应专题。
- 课程总览增加章节知识图谱，支持按章节查看知识点结构并进入学习。
- 预习、巩固、冲刺按当前章节提供差异化学习内容与 AI 指令。
- 教材原文排版调整为更适合长文阅读的教学文档样式。

### AI 与 RAG

- AI 回答统一采用流式输出，减少生成等待感。
- 回答使用受限 Markdown 教学文档渲染，支持标题、列表和加粗。
- 展示教材原文依据与引用位置，避免无来源的泛化回答。
- 悬浮 AI 助教增加章节/阶段上下文绑定，并支持拖动与位置记忆。

### 学习任务与进度

- 新增学习任务点、用户任务进度和学习行为事件数据表。
- 根据阅读进度、AI 使用、保存笔记等实际学习行为自动更新任务点。
- 阶段页面不再依赖手动勾选；首页综合进度自动汇总任务点完成情况。

### 笔记空间与复习

- 新增笔记空间，笔记与教材、章节、用户强绑定，仅本人可见。
- 笔记索引由左侧目录改为顶部横向专题导航，扩大正文编辑空间。
- 新增笔记 AI 助手，支持基于当前章节教材与个人笔记的流式问答。
- AI 问答按用户、课程、章节保存服务端历史记录，并保留教材引用来源。
- 间隔复习按 1、2、4、7、15、30 天计划运行。

### 时政要点

- 时政要点接入人民网时政、中国新闻网时政/要闻、央视新闻国内等公开 RSS 来源。
- 增加来源标签和“刷新时政”按钮，所有登录用户都可手动刷新。
- 页面重新可见且超过 30 分钟后自动拉取最新资讯。
- 修复 GB 系列 RSS 编码兼容，以及跨订阅流重复文章导致的刷新失败问题。
- 时政 AI 总结与课本关联改为流式输出，并沿用教材化排版和引用展示。

### 验证与部署

- 后端测试通过（18 项）。
- 前端 TypeScript 检查与生产构建通过。
- 已部署至测试服务器，并完成健康检查、任务表创建、笔记对话表创建及多来源时政抓取验证。

## 2026-07-16

### 首页与课程总览

- 首页重新组织为 AI 思政学习工作台，突出继续学习、教师待完成任务、学习进度和常用学习入口。
- 增加 AI 学习伙伴组件与扩展接口，为后续替换正式宠物形象、状态和交互能力预留统一数据结构。
- 课程总览改为“教材主线 + AI 学习路径”视觉结构，保留专题学习、时政要点和课堂互动入口。
- 管理员可在教材创建后修改教材名称与简介，解决导入后“暂无简介”无法维护的问题。

### 教师任务与学习进度

- 新增教师学习任务模块，支持按教材、专题、学习阶段和任务类型发布任务。
- 支持面向全部学生或指定学生发布，学生首页突出展示待完成、进行中和逾期任务。
- 教师可以查看任务总人数、完成数、进行中人数和逾期人数，并可撤回本人发布的任务。
- 学生完成阅读、AI 辅助或保存笔记后，任务进度根据已有任务点自动同步，无需手工勾选。

### 笔记空间

- 笔记空间视觉升级为知识工作台，保留专题索引、富文本编辑、预览、导出、教材关联和 AI 问答能力。
- 增加可折叠侧边工具栏，格式设置移动至编辑区顶部，避免遮挡笔记正文。
- 富文本编辑支持标题、列表、加粗、斜体、下划线、字号和记号笔标记等常用格式。
- 笔记编辑区与 AI 对话区支持拖动调整宽度，内容随面板宽度自适应。
- 笔记保存后建立个人向量索引，支持语义搜索、相关章节笔记和相关教材段落推荐。
- 增加 AI 写作辅助、章节会话历史、Markdown/Word 导出和间隔复习题闭环。
- 笔记、AI 会话、语义检索和复习记录均按用户与专题隔离。

### 时政要点与教材联动

- 时政列表增加关键词搜索、来源筛选、时间范围、排序和分页，支持按不同媒体快速筛选。
- 清洗 RSS 摘要中的 HTML、图片、表格和异常字符，修复搜索结果中标签与图片乱码问题。
- 教材关联由固定章节调整为跨全书章节匹配，并展示候选专题、关联理由、教材摘录和正文位置。
- 用户可将时政 AI 总结与教材关联整理为研学笔记草稿，确认后新建或追加到对应专题笔记。
- 保存时政笔记时保留媒体名称、原文链接和发布时间，保证学习材料可追溯。

### AI 助教体验

- 悬浮 AI 助教在收起状态和展开状态下均支持拖动，并保留用户上次放置位置。
- 悬浮入口和对话窗口统一为 AI 思政视觉语言，增强入口辨识度与页面一致性。
- AI 回答继续使用流式输出、受限富文本渲染和教材引用卡片，减少等待感并避免原始 Markdown 符号直接显示。

### 工程验证与项目体检

- 后端完整测试通过（24 项），覆盖账号、教材专题、知识库、学习任务、教师任务、笔记复习和时政搜索等主要流程。
- 前端 TypeScript 检查与生产构建通过，确认当前页面和新增组件可正常打包。
- 识别到 Element Plus 生产包体积较大，后续将通过按需引入和拆包优化首屏资源。
- 确认下一阶段不再优先堆叠新功能，转向数据库迁移、权限边界、异步教材处理、RAG 评测和自动化部署。

### 下一阶段优先事项

- 引入 Alembic，建立 MySQL 数据库基线、版本迁移和回滚流程。
- 增加教学班、班级成员和教师归属，避免“全部学生”跨班级混用。
- 教师注册增加管理员审核或邀请码机制，并为登录、AI、文件上传和时政刷新增加限流。
- 教材上传调整为后台解析与向量化任务，支持进度、失败重试和扫描版 PDF 提示。
- 为全书专题建立 RAG 回归评测集，补充真实页码、小节和段落级引用。
- 增加 GitHub Actions、前端端到端测试、版本标签和服务器回滚记录，保证本地、GitHub 与测试服务器版本一致。

## 2026-07-17

### 教学班与教师审核

- 将课程科目、教材和教学班拆分建模；教学班绑定学期、主教材、补充教材、主讲教师和协作教师。
- 教师注册后进入管理员审核流程，未审核教师只能查看审核状态，不能进入教学功能。
- 学生支持班级码申请和 Excel/CSV 名单匹配；学号统一转为大写后精确匹配，不根据编号外观猜测身份。
- 数据库保证同一学生在同一课程科目、同一学期只有一个当前教学班；默认试点班可直接迁入正式班，正式班之间转班需主讲教师审批。
- 支持自由退出、退出审批、重新加入、历史班级只读查看、随机分组和名单指定分组；转班不删除个人笔记、AI 会话和既有学习记录。
- 教师任务与课堂互动绑定具体教学班，支持全班、指定小组、指定学生三种发布范围，修复全班任务可能误发给其他班学生的问题。

### 精确教材引用

- 教材导入保留 PDF 物理页、印刷页、章节路径、知识点、页内段落和原文锚点，引用卡片区分主教材与补充资料。
- 新增教材校准工作台，同屏提供教材结构、PDF 原页和节点属性，可调整章、节、知识点边界及罗马/阿拉伯印刷页码区段。
- 教材需经过“待校准—已校准—已发布”流程；管理员发布后仍保留教材版本，不覆盖旧版本引用。
- AI 助教、笔记 AI 和时政教材关联的引用卡片可打开原始 PDF 页，并在侧边文字中突出引用片段；无法精确高亮时保留原页跳转与摘录核对。
- 增加仅引用页、全文预览、允许下载三种资料权限，以及引用位置反馈接口。
- AI Prompt 要求关键结论使用资料编号，主教材优先；补充资料表述不一致时必须明确提示。

### 向量索引与迁移

- 阿里百炼 Embedding 默认升级为 `text-embedding-v4`、1024 维，沿用现有 API Key 和兼容模式 Base URL。
- 新增版本化 Chroma 安全重建脚本：先在新 collection 构建并核对数量，再通过指针文件原子切换，旧索引保留用于回退。
- 引入 Alembic 基线迁移，覆盖教学班、教师审核、教材版本、分页结构、精确向量块和引用反馈数据表。
- 提供默认试点班迁移、既有 PDF 精确引用补建及向量重建脚本，支持现有 MySQL 数据平滑升级。

### 本轮验证

- 前端 TypeScript 类型检查通过。
- 前端 Vite 生产构建通过。
- 后端完整回归测试通过（28 项），新增覆盖教学班隔离、同课程转班规则、精确引用页权限、罗马印刷页码和引用元数据。
- Alembic 在全新 SQLite 数据库完成 `upgrade head` 验证，前端 Vite 生产构建通过。

## 2026-07-18

### 分层权威资料中心

- 将原教材知识库升级为“中央材料—教材正文—地方材料”三层资料中心，原教材上传、版本更新、历史版本、自动专题拆分和引用校准能力完整保留。
- 中央材料仅管理员可通过文件或公开 HTTPS 原文导入，必须填写发布机关和发布日期，并人工确认适用教材、专题与知识点后才能发布。
- 地方材料支持管理员和已审核教师导入；教师资料必须绑定本人教学班，并至少关联一本教材或一个专题，防止资料跨班级泄漏或形成孤立索引。
- 新增资料版本替换、归档、内容哈希核验、原网页快照、历史资料待分类和权限化索引重建；归档版本保留历史核对能力，但不再参与新回答。

### 权威检索与真实引用

- AI 检索先执行专题相关性过滤，再叠加中央材料、教材正文、地方材料的权威权重，避免高权威但无关的材料挤占当前专题正文。
- 单轮证据按“中央最多 2 条、教材至少 2 条、地方最多 1 条”组织，其余名额只由相关教材正文补足；资料不足时明确说明，不拼凑、不伪造来源。
- 检索资格同时校验当前教材版本、人工确认范围、资料发布状态和学生教学班，旧版、待确认、已归档或越权资料不会参与新回答。
- AI Prompt 明确禁止编造标题、发布机关、日期、文号、链接、页码和资料编号；服务端再次依据数据库校验引用卡片，失效来源不会返回前端。
- AI 助教、笔记空间和时政关联统一显示资料层级、发布机关、日期与真实引用入口；PDF 打开校准原页，网页打开权威原文。
- 个人笔记向量集合增加模型与维度版本标识，并提供重建脚本，修复从 1536 维模型切换到 1024 维 `text-embedding-v4` 后的语义检索维度冲突。

### 本轮验证

- 后端完整回归测试通过（35 项），新增覆盖中央材料发布门槛、地方资料教学班隔离和分层检索配额。
- Alembic 在全新 SQLite 数据库从基线升级到 `20260718_02` 成功。
- 前端 TypeScript 类型检查与 Vite 生产构建通过；构建仅保留既有 Element Plus 大包体积提示。

## 2026-07-23

### 前端设计系统与公共布局

- 新建设计令牌、Element Plus 主题覆盖、基础样式和通用工具类，将品牌色、字号、间距、圆角与阴影集中管理。
- 新增 `UiHero`、`UiCard`、`UiPageHeader`、`UiStatCard` 等无业务逻辑公共组件，为后续页面逐步迁移提供统一基础。
- 主布局升级为 64px 顶栏；桌面端保留完整导航，1024px 以下提供菜单按钮与移动端抽屉，确保主要模块始终可访问。
- 保留未迁移页面的兼容样式，仅清理课程详情中已停用的旧轨道动画和悬浮指标规则，降低一次性改造风险。

### 首批页面改造

- 登录页改为教材学习场景介绍与登录表单组合布局，表单宽度使用流式约束，补充自动填充、键盘焦点和完整表单语义。
- 学习工作台重组继续学习、待完成任务、当前学习状态、阶段入口、AI 学习建议和快捷入口，教师与学生的既有业务分支保持不变。
- 课程详情统一为教材概览、学习阶段摘要、内容导航、专题卡和学习路径；教材版本、上传、引用校准、专题管理和知识图谱功能全部保留。
- 手机端专题卡改为“专题编号与标题—状态—三阶段按钮”分行布局，解决 390px 宽度下标题逐字竖排和按钮挤压问题。
- 静态预览稿同步修复登录卡横向溢出、移动端 Hero 过度换行、专题卡布局、预览切换条滚动及标签关联问题。

### 响应式与构建验证

- 在 390px、768px、1024px、1280px 四档检查页面宽度，未发现横向滚动。
- 390px 专题标题保持横排，三个阶段按钮等分排列；登录卡在窄屏内自适应。
- 前端 TypeScript 类型检查通过。
- Vite 生产构建通过；仅保留既有 Element Plus 包体积提示，后续继续按需引入与拆包。

### 教师任务完成详情

- 教师与管理员可从已发布任务进入学生完成详情，按姓名、学号或小组检索，并区分未开始、进行中、已完成和已逾期状态。
- 详情页展示任务点完成进度和最近学习活动时间，打开期间每 30 秒自动同步，同时保留手动刷新入口。
- 支持一键导出未完成学生 CSV 名单，便于教师线下提醒和课堂点名；导出内容对表格公式注入做了安全处理。
- 权限校验限定为管理员、任务发布教师或该教学班任课教师；页面不展示学生私人笔记正文或 AI 对话内容。

## 2026-07-28

### 重大升级第一阶段：V0 体验基线

- 根据 V2.0 升级方案固定学生“学习—反馈”、教师“备课—发布”、管理员“发现—审核”三条核心路径。
- 将统一顶栏改造为桌面端 224px 角色侧栏与 64px 上下文栏；每个角色最多保留五个一级入口，1024px 以下切换为移动端抽屉。
- 上下文栏集中显示当前课程、专题或教学班，并预留全局搜索、消息提醒和后台长任务入口。
- 首页按角色拆分为学生今日学习、教师教学工作台和管理员平台概览，避免不同角色功能继续堆叠在同一页面。
- 新增课程备课工作区，固定“设置任务—构建证据—生成课纲—生成成果—预览发布”五步链路；第一阶段完成教学范围与证据包确认，后续接入可追踪 Agent 任务。
- 新增资料审核工作区，以“候选资料—原文证据—审核决定”组织人工审核；索引失败或正文分块为空的资料禁止发布。
- 新增状态标签、任务卡、证据卡、步骤进度和空状态组件，并将权威红、操作蓝、提示金等颜色收敛到设计令牌。
- 补充 `docs/v0-experience-baseline.md`，记录角色导航、状态语义、证据规则与验收口径。

### 本轮验证

- 前端 TypeScript 类型检查通过。
- Vite 生产构建通过；仅保留 Element Plus 既有大包体积提示。
- 本地登录页可以正常渲染；因本地 FastAPI 未启动，角色工作台的带数据浏览器联调留待启动完整环境后继续。

### 重大升级第一阶段：教师备课 Agent 闭环

- 采用“按角色呈现能力、后端共享一套 Agent 运行引擎”的结构，首个落地场景为教师备课；本阶段未拆分三套重复接口，也未开放 Agent 自动发布权限。
- 新增持久化 `AgentRun` 与 `AgentStep`，记录任务类型、上下文、证据快照、步骤状态、模型、Prompt 版本、输出、失败原因与重试关系。
- 教师备课流程升级为“设置教学任务—构建并冻结证据快照—教师人工确认—后台生成结构化课纲—预览草稿”，所有课纲均明确标记为尚未发布。
- 证据包复用现有分层权威检索与真实引用机制；Prompt 限定只能依据已确认快照生成，不允许补造资料、页码、标题或资料编号。
- 课纲输出包含课程定位、知识/能力/价值目标、重点、难点、按课时分配的教学流程、讨论问题、课后任务和引用说明；当前先完成课纲，PPT 等成果生成仍保留为下一步骤。
- 新增 Agent 任务列表、详情、确认、取消、重试与 SSE 状态流接口；顶栏后台任务抽屉可查看历史任务并返回备课工作区。
- 进程重启时会识别未完成任务并标记为可重试失败，避免任务永久停留在“运行中”；高并发生产环境后续再平滑替换为 Redis/Celery 队列。

### Agent 闭环验证

- 后端完整回归测试通过（48 项），覆盖教师/管理员权限、学生禁止访问、证据确认、结构化课纲生成、SSE 完成事件、取消与重试。
- Alembic 在全新 SQLite 数据库从基线升级到 `20260728_01` 成功。
- 前端 TypeScript 类型检查与 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。
- 浏览器完成真实交互验收：管理员选择课程专题并构建证据包，人工确认后生成 90 分钟课纲，后台任务中心显示“已完成”，页面保持“教师草稿、尚未发布”状态。

## 2026-07-29

### 教师备课 Agent：教学成果生成

- 在教师确认课纲后增加成果生成步骤，可按需选择 PPT、教案和课堂活动方案，任务进入后台执行，教师可以离开页面后再从任务中心返回查看。
- PPT 生成采用结构化内容与可编辑 `.pptx` 文件，内置课程标题页、教学目标、重点难点、内容讲授、课堂讨论和总结应用等版式；教案与课堂活动生成可编辑 `.docx` 文件。
- 成果预览与下载均复用现有 Agent 运行记录，不增加重复业务表；接口只返回受权限保护的下载地址，不暴露服务器存储路径。
- 所有成果保持“教师草稿、尚未发布”，本阶段不允许 Agent 自动发布，教师仍需人工核验。
- PPT 的学生可见页面不显示“资料依据”或 `[资料N]` 等系统引用标记，避免影响课堂展示；真实证据编号只写入演讲者备注和后台元数据，保留教师核验与审计能力。
- Prompt 与服务端双重约束 PPT 可见内容：即使模型输出引用标记，生成文件前仍会统一清洗，防止不同模型输出习惯造成格式回退。

### 本轮验证

- 后端成果生成专项测试通过（5 项），覆盖三类成果生成、结构预览、权限化下载、课纲前置条件和 PPT 可见引用清理。
- PPT 文件完成逐页渲染与越界检测，8 页示例未发现元素溢出；标题页和正文页完成视觉检查。
- 前端 TypeScript 类型检查与 Vite 生产构建通过。
- 浏览器完成“确认课纲—选择成果—后台生成—在线预览—文件下载入口”完整流程验收。

## 2026-07-30

### 教师备课 Agent：个性化 PPT 设计

- 将 PPT 从“自动套用多种模板”调整为二阶段 Agent：内容策划 Agent 先形成逐页教学叙事，视觉设计 Agent 再针对本次专题提炼独有视觉母题、气质、配色和页面构图。
- 视觉设计 Agent 输出 16:9 自由画布，以百分比坐标安排标题、结论、要点、形状和关系线；文字只能引用已经确认的页面内容，不能借视觉设计阶段增加事实。
- 服务端对颜色、坐标、字号角色、元素类型、画布边界和有效页数进行校验；设计结果不完整时才启用原有结构化安全版式，不让单次模型异常阻断成果生成。
- PPT 渲染器根据自由画布使用 PowerPoint 原生文本框与形状生成可编辑文件，主题颜色由当前专题决定，不再固定为一套思政红、教学蓝模板。
- 教师可见 PPT 页面继续隐藏 `[资料N]` 与“资料依据”等系统标记，真实引用只保存在演讲者备注和后台元数据中，不影响课堂展示。
- 前端成果预览同步渲染个性化自由画布，并展示本次课的视觉方向和是否触发安全回退。

### 本轮验证

- 教师备课 Agent 成果生成专项测试通过（5 项），测试模式同样经过自由画布渲染链路。
- 生成 10 页自由画布示例 PPT，完成逐页渲染、视觉检查和元素越界检测，未发现文本或图形溢出。
- 前端 TypeScript 类型检查与 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。

### 教师备课 Agent：PPT 质量闭环与用户模板

- 新增 PPT 生成偏好：使用场景、视觉风格、内容密度、页数范围和课堂互动均由教师在生成前设定，并传递给内容策划与视觉设计 Agent，不再通过切换固定模板间接控制结果。
- 新增第三阶段质量审查。系统合并规则检查与模型审查结果，对自由画布回退、文字拥挤、关键信息遗漏、课堂互动缺失等问题给出分数和修改建议。
- 新增单页修改流程，教师可选择“调整内容”或“仅重新设计版式”，每次只重绘目标页并重新检查整套 PPT，避免修改一页却重新生成全部内容。
- 新增 PPT 历史版本，完整重生成、单页修改与版本恢复前均自动归档，默认保留最近 10 个版本。
- 新增 `.pptx` 风格模板上传、解析、选择和删除接口。系统提取画幅比例、主题色、字体和版式名称供视觉设计 Agent 使用；首版明确定位为风格参考，不宣称复制复杂母版、动画或嵌入对象。
- 辅助插图选项保留为禁用状态和后续接口入口；在未配置图片生成模型前不生成占位图，也不伪装为多模态能力。

### 本轮验证

- 后端全量回归测试通过（52 项），覆盖模板上传与选择、质量报告、单页修改、版本归档与恢复。
- Alembic 在全新 SQLite 数据库从基线升级至 `20260730_01` 成功。
- 前端 TypeScript 类型检查和 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。
- 本地浏览器完成备课页验收：生成偏好、模板上传说明、逐页修改入口和历史成果均可访问，既有备课数据与下载能力未受影响。

## 2026-07-31

### 教师备课 Agent：多模态课件与发布闭环

- 课程备课由三列拥挤布局调整为五阶段工作区，一次只呈现当前阶段；教师可在已完成步骤间返回核验，未满足前置条件的步骤不能提前进入。
- PPT 页数由两个范围输入框改为 6～30 页精确控制，支持加减与直接输入；Prompt 和服务端均执行精确页数约束，模型返回页数偏差时自动安全收敛。
- 接入阿里云百炼原生多模态图像接口，默认使用可配置的 `wan2.7-image`。视觉 Agent 只为少量合适正文页申请辅助插图，图片立即本地化，单页失败自动回退为原生图形。
- 多模态配图增加教学安全边界：不生成政治人物肖像、国旗国徽、公章、政策文件原件、伪新闻摄影、文字和二维码。
- 新增“预览发布”最终步骤，教师可选择 PPT、课堂讨论及具体讨论项，完成最终确认后一次发布到指定教学班；同一备课任务不能重复发布到同一教学班。
- 新增教学成果发布记录与权限化下载。学生和任课教师可在课堂互动页查看教师发布的课程成果并下载 PPT；所选讨论同步进入原有课堂互动列表。
- 新增 `20260731_01` 数据库迁移和多模态能力探测接口，前端仅在服务配置可用时开放“生成辅助插图”。

### 本轮验证

- Agent、模型表、精确页数、多模态资源挂载、PPT/讨论发布及学生下载专项测试通过（12 项）。
- 前端 TypeScript 类型检查和 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。
- 本地页面已确认五阶段导航和单阶段内容结构正确；登录态随后失效，完整带数据视觉与发布联调仍需使用有效本地登录会话。

## 2026-08-01

### 全局思政 AI 助教：Chat / Agent 双模式

- 新增统一的 `/api/v1/ai/workspace/stream` 流式入口，Chat 用于教材约束问答，Agent 用于任务拆解、备课建议和待确认项整理；两种模式均复用现有分层 RAG 和原文引用链路。
- 增加学生、教师、管理员角色化提示约束。服务端根据登录身份兜底角色，学生不能通过请求体伪装为教师或管理员；发布、删除、导入、通知等有副作用操作不会被 AI 宣称已执行。
- 全局助手不再局限于预习、巩固、冲刺页面，可在登录后的课程、时政、笔记和备课页面打开；未绑定教材专题时明确提示，不伪造教材依据。
- 新增阿里云风格助手面板：欢迎区、能力卡片、Chat/Agent 切换、会话历史、本章上下文和教材依据标签。窗口默认支持自由拖动，也可锁定为右下角固定模式，位置和布局偏好保存在浏览器本地。
- PPT 辅助插图继续复用已接入的阿里云多模态接口；教师在备课成果生成阶段勾选“生成辅助插图”后由后台 Agent 调用，单图失败自动回退为原生版式，不阻断整套 PPT。

### 本轮验证

- 后端全量回归测试通过（58 项）；新增工作台无上下文、角色兜底和带教材上下文流式接口测试。
- 前端 TypeScript 类型检查和 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。
- 本地页面确认登录页不显示助手，登录态下由主布局统一挂载全局助手；完整带教材数据的浏览器验收需使用有效本地登录会话。

### 全局 AI 工作台：自动教学范围与效率 Agent

- 将全局助手从右下角悬浮入口调整为主布局顶部的固定工作台。工作台默认收起，不遮挡课程、笔记、时政等业务页面；展开后可在同一位置完成 Chat 或 Agent 操作。
- 新增可解释的教学范围识别：优先读取当前页面的教材/专题参数，其次读取用户手动选择、最近学习记录、教师任务和默认教学班。页面教材优先级高于默认教学班；当默认班未绑定该教材时，不会误带入后续备课或发布流程。
- 新增“教学范围”面板，展示识别来源和置信状态，并允许用户一键切换“教学班 + 教材”及专题。AI 不再默默猜测当前教材，跨教材、跨教学班使用时可人工纠偏。
- 将 Agent 与 Chat 做实质分工：Chat 继续用于带教材依据的问答；Agent 会先返回上下文、执行计划和进度，再按意图创建备课草稿、整理个人待办、汇总教学任务、引导资料管理或准备批改量规。
- 教师/管理员提出课纲、PPT、教案或课堂互动需求时，Agent 只创建可追踪的备课草稿并跳转到对应任务；教师仍需核验证据、选择成果并最终发布。学生不能通过前端请求伪装为教师或管理员。
- Agent 增加角色化快捷入口：教师可发起课纲/PPT、课堂互动、任务设计、学情速览和批改量规；学生可制定学习计划、查看待完成任务、进入预习/复习/冲刺；管理员可检查资料和索引范围。
- 修复流式显示的响应式更新路径：流式回调改为按消息 ID 更新 Vue 响应式数组中的对象，避免请求成功但前端 AI 气泡一直空白。
- 备课页支持读取 `run_id` 路由参数。由 Agent 创建草稿后点击“打开备课草稿并核验证据”即可准确进入该任务，而不是回到最近一次无关任务。

### 本轮验证

- 后端全量回归测试通过（60 项），新增教学范围页级优先级与教师 Agent 创建可追踪备课草稿的流式测试。
- 前端 TypeScript 类型检查与 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。

## 2026-08-02

### 规划型 Agent：工具编排与执行闭环

- 将规划器的工具注册表扩展为可解释的教学工具集：读取教学上下文、读取任务、读取学生任务点与笔记状态、检索教材/权威资料、检查备课就绪状态，以及复用现有备课工作流生成课纲、PPT、教案和课堂互动。
- 增加角色工具边界。学生只能读取自己的学习状态、任务和教材资料，不能调用教师备课、教学任务或教学成果生成工具；工具权限在服务端过滤，不依赖前端传入的角色。
- 工具执行改为“计划—运行—完成/阻塞—总结”四段式流式协议。每一个步骤都会回传实时状态，工具结果不会直接堆叠在对话中，而是由 Agent 统一整理为结论、已完成事项、待确认事项和下一步操作。
- 增加确定性回退和失败补救。大模型规划不可用时仍按问题意图生成最小安全计划；检索、任务状态或学习计划工具失败时明确说明原因并给出可执行的课程、专题或备课入口，不会把草案误报为已发布结果。
- 学习计划会结合当前专题任务点、阶段进度和个人笔记状态动态生成；资料检索会保留来源标题、位置和相关性信息，供 Agent 总结时引用。
- 备课就绪检查会识别证据确认、后台生成、课纲完成及成果缺失状态，并根据当前状态提供确认、重试或进入预览的动作。
- 明确的备课、任务、资料检索等指令改为本地快速规划，不再先阻塞等待大模型决定工具；多步骤工具完成后直接回传结构化结果，避免“工具已完成但仍在等待模型总结”造成空白。
- Agent 面板新增常驻执行状态卡：请求建立计划、调用工具或后台等待时展示旋转状态标识、当前步骤和说明；规划异常时会回退为基础范围检查并明确显示阻塞原因。

### 本轮验证

- 后端全量回归测试通过（71 项），新增学生学习状态、无上下文选题和规划器结果总结测试。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有 Element Plus 包体积提示。

### PPT 多模态插图链路增强

- 设计 Agent 未输出 `image` 占位时，系统会根据页面内容自动选择 1～3 个适合插图的正文页，跳过封面和总结页，并在不遮挡正文的空白区域补充视觉槽位。
- 辅助插图优先用于概念关系、过程路径、时间线、对比和课堂互动页面，避免把图片当作无意义装饰。
- 百炼图像请求增加 429/5xx 和连接异常的轻量重试，仍以单页失败回退为原则，不阻断整套 PPT 生成。
- 默认多模态模型更新为可配置的 `wan2.7-image-pro`；可在 `.env` 中改为 `wan2.7-image` 以降低调用成本。API Key 继续复用 `DASHSCOPE_API_KEY`，也支持单独的 `PPT_MULTIMODAL_API_KEY`。
- 前端在多模态能力可用时默认勾选“生成辅助插图”，教师仍可手动关闭；生成完成后显示实际插图页码。

### 本轮验证

- 后端全量回归测试在 SQLite 隔离环境通过（72 项）；直接使用本机 MySQL 配置时因沙箱禁止连接 `127.0.0.1:3306`，未将该环境错误计入代码失败。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有 Element Plus/Rollup 包体积提示。
- 当前本地 `.env` 未配置百炼 Key，因此尚未执行真实图片生成调用；服务器配置 `DASHSCOPE_API_KEY` 后即可联调。

### 最终 Agent 执行闭环升级

- 新增工作台 Agent 执行记录表 `agent_executions` 与 `20260802_01` 迁移。每次 Agent 请求都会持久化角色、教材/专题/教学班范围快照、工具计划、工具结果、最终结论、告警与重试关系，刷新页面不会丢失执行上下文。
- 新增 Agent 任务中心接口及前端入口，可查看近期任务的状态与摘要；已完成或异常任务可“基于此任务重试”。重试时强制沿用原任务的教材与专题快照，不会因用户切换页面误落到另一门课程。
- 统一工具结果协议：每项工具返回状态、摘要、结构化数据、后续动作、告警和是否可重试。某个工具失败不会吞掉其他已完成结果；执行记录保留告警，用户可以在任务中心继续处理。
- 服务重启时，未完成的工作台任务会自动标记为可重试失败，避免任务中心长期显示“执行中”。既有 `AgentRun` 备课任务恢复策略不变。
- 扩展高频效率工具：教师/管理员可直接由 Agent 生成教材约束的批改量规、生成未完成任务的提醒草案（不自动发送）、检查中央材料/教材/索引健康状态；所有发布、提醒和评分仍需人工确认。

### 本轮验证

- SQLite 隔离环境后端全量测试通过（73 项）；新增 Agent 执行持久化、任务查询和基于原范围重试覆盖。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有 Rollup PURE 注释和 Element Plus 包体积提示。

### Agent 状态机、结果校验与角色模板完善

- 补齐 `waiting_confirmation` 状态。备课证据创建完成后，工作台任务会暂停等待教师确认；教师可直接在 Agent 对话或任务中心确认/取消。确认接口会校验关联备课任务的真实状态，不能只修改界面状态绕过证据确认。
- 新增确定性执行校验器，不由大模型自行宣称任务已完成。系统会检查是否产生可读结果、计划工具是否成功、教材专题是否可验证，以及副作用操作是否保留人工决策，并把校验项、告警和阻塞动作写入执行记录。
- 任务中心展示等待确认、执行异常、已完成等状态；等待确认任务可直接继续或取消。取消备课类 Agent 任务时会同步取消关联的备课草稿，避免留下仍在等待的孤立任务。
- 新增服务端角色任务模板。学生、教师、管理员分别获得符合权限的高频入口，前端不再自行决定角色能力；学生不会收到 PPT、批改或教师提醒工具。点击快捷任务会明确进入 Agent 模式，和 Chat 教材问答保持分工。
- Agent 结果卡新增执行校验与告警展示，让用户能够判断哪些步骤真实完成、哪些结果需要确认或重试。

### 本轮验证

- SQLite 隔离环境后端全量测试通过（74 项），覆盖等待确认、真实备课状态校验、执行恢复和角色模板权限。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有构建体积提示。
## 2026-08-03

### 权威资料发现第二阶段：关联与差异证据

- 候选材料自动扫描全部教材专题，生成可解释的课程、专题和知识点建议范围；建议与管理员最终确认范围分离。
- 新增 `policy_changes` 证据表，按句段对比教材正文和已发布中央材料，保存新旧原文、来源、相似度、变化类型、重要程度和提醒建议。
- “资料动态”页面增加教材关联与政策变化区域，支持重新分析、查看证据、确认并提醒教师、加入观察和标记误判。
- 新增数据库迁移 `20260803_02_policy_changes.py`，本地单元测试 80 项通过，前端类型检查与生产构建通过。

### Embedding 维数与索引版本治理

- 恢复阿里百炼 `text-embedding-v4`、1024 维，并通过真实接口验证实际返回维数。
- 新增 Embedding 配置指纹，主知识库与个人笔记 collection 同时隔离提供方、模型、实际维数和索引协议版本。
- DashScope `text-embedding-v1/v2` 固定识别为 1536 维，`text-embedding-v3/v4` 使用显式维数；每批 API 响应都会校验实际维数。
- 活动索引清单升级为带模型指纹的版本化格式；模型或维数不一致时拒绝复用旧集合，并给出安全重建提示。
- 全量重建增加就绪文档缺块检查、总分块核对，以及中央材料、教材、地方材料分层覆盖率校验；全部通过后才允许原子切换。
- 切换前自动保存上一份活动索引清单，旧 collection 保留用于匹配配置后的回滚。
- 后端完整回归测试通过（78 项），真实 DashScope Embedding 连通测试返回 1024 维。

### 权威资料发现第一阶段

- 增加权威来源白名单、发现任务、候选材料和正文快照四类数据表，保留来源等级、抓取周期、失败次数、内容哈希和解析器版本。
- 接入中国政府网、教育部、求是网三类默认来源，支持 HTML 栏目、RSS、Sitemap 适配器；抓取前校验 HTTPS、公网地址和白名单域名。
- 新增后台发现任务：按主题词读取来源入口、抓取正文、规范化 URL、正文哈希去重，并将候选材料停留在待审核池，不会绕过人工确认进入正式 RAG。
- 候选材料保存正文预览和独立快照，可由管理员查看原文、驳回、标记重复，或选择教材后复用中央材料流程发布并建立索引。
- 新增“资料动态”管理员页面，展示来源状态、任务进度、候选材料和发布操作；页面关闭不影响后台任务。
- 预留按来源抓取周期的调度器，默认关闭，管理员确认来源白名单后可通过 `AUTHORITY_DISCOVERY_SCHEDULER_ENABLED=true` 开启。

### 权威资料发现第三阶段：发布同步与教学提醒

- 新增政策变化知识库同步状态：`pending`、`waiting_publish`、`synced`、`failed`，确认变化不会绕过中央材料的管理员发布门槛。
- 候选材料发布后，已确认的变化会使用当前 Embedding profile 重建对应文档索引；失败会保留错误信息，并提供管理员重试接口。
- 新增 `teaching_notifications` 站内提醒表和 `/api/v1/notifications` 接口；只向关联教材教学班的已审核教师发送，未建立教学班绑定时回退到已审核教师，且重复任务不会重复提醒。
- 顶部消息入口从静态占位改为带未读数、批量已读和定时刷新（60 秒）的教学提醒抽屉，消息包含变化级别、证据摘要和原文网址。

### 本轮验证

- 后端全量回归测试通过（80 项）；前端 TypeScript 检查通过。
- SQLite Alembic 迁移已执行到 `20260803_03` 的 DDL 阶段；本机 MySQL 连接受当前沙箱网络权限限制，未进行真实 MySQL 连接验证。

### 本轮验证

- 后端全量回归测试通过（79 项）；新增权威来源发现、正文快照和候选入池测试。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有构建体积提示。

## 2026-08-04

### 权威资料发现任务限流与后台调度稳定性

- 将“每个任务独立创建一个后台线程”改为进程内受控调度器：任务先进入 `queued`，由单一 dispatcher 按 `AUTHORITY_DISCOVERY_MAX_RUNNING` 启动有限数量的执行线程，避免重复点击或批量请求产生大量等待线程。
- 增加任务队列上限 `AUTHORITY_DISCOVERY_MAX_QUEUED`。达到上限时接口返回 429，并明确提示管理员稍后重试；已排队任务仍可在页面关闭后继续执行。
- 增加同条件手动任务冷却 `AUTHORITY_DISCOVERY_COOLDOWN_MINUTES`，按用户、来源、关键词、日期范围和查询词判重，避免相同发现任务反复抓取。
- 增加单来源线索上限 `AUTHORITY_DISCOVERY_MAX_LINKS_PER_SOURCE`、全局请求最小间隔 `AUTHORITY_DISCOVERY_REQUEST_INTERVAL_SECONDS` 和每日正文抓取上限 `AUTHORITY_DISCOVERY_DAILY_FETCH_LIMIT`，降低对权威站点、数据库和大模型接口的瞬时压力。
- 发现任务调度器继续默认关闭；开启前建议单进程部署（Uvicorn workers=1），后续扩容到多实例时再接入 Redis 分布式队列和分布式锁。
- “资料动态”页面在任务运行期间禁用“启动发现”按钮，避免用户无意中重复提交；失败任务仍可单独重试。
- 未预期的后台异常会被写回任务为 `failed` 并释放运行槽位，不再留下永久 `running` 任务；服务重启时遗留的运行中任务会自动重新排队。

### 本轮验证

- 后端全量回归测试通过（85 项），覆盖候选入池、无关正文过滤、任务冷却和队列上限。
- 前端 TypeScript 检查与 Vite 生产构建通过；仅保留既有 Rollup PURE 注释和包体积提示。

### 权威资料发现三阶段闭环复核与补强

- 第一阶段补齐来源新增、编辑、启停，抓取/提醒权限和请求间隔配置；发现任务支持停止、错误查看与失败重试。
- 发现策略由“标题关键词硬过滤”改为“标题优先、正文复核”。正文不含检索主题的材料不再因来源等级较高而误入候选池。
- 第二阶段将教材关联升级为 BM25、活动向量索引、标题命中和正文重叠的混合召回，并由大模型在合法候选专题 ID 内受约束复核；向量或模型不可用时保留确定性降级路径。
- 修复多教材中央材料通过 `document_course_scopes` 保存后无法参与新旧表述对比的问题。
- 第三阶段遵循来源的 `allow_alert` 设置；管理员在新候选分析完成后收到待审提醒，教师提醒提供可访问的权威原文入口，不再指向无权限的管理员页面。
- 新增无关正文过滤、多教材中央材料对比、来源关闭提醒三类回归测试。

### 权威资料发现并发闭环补强

- 发现任务创建与定时调度共享进程内创建锁，避免多个请求同时通过“队列未满/冷却期未命中”检查后重复创建任务。
- dispatcher 启动任务前通过数据库条件更新原子抢占 `queued` 任务；即使误用多个 Web Worker，同一任务也只有一个进程能进入执行阶段。
- 后台任务完成或异常释放运行槽位后会立即唤醒 dispatcher，不再固定等待下一轮空闲轮询。
- “资料动态”页面只在存在排队中或运行中的任务时，每 10 秒轻量刷新任务列表；标签页不可见或任务结束后停止请求，避免空闲页面持续占用 API 和数据库连接。
- 增加任务原子抢占回归测试；后端全量测试 86 项通过，前端 TypeScript 检查与 Vite 生产构建通过。

### 权威资料发现质量筛选与任务可解释性

- 将来源等级从相关度计算中移除，拆分主题相关度、教材关联度、正文质量和教学重要度，解决全量巡检固定显示 35% 的问题。
- 增加正文质量评估：优先使用文章/正文容器，过滤导航、栏目目录和推荐链接占比过高的页面；正文质量不足的页面不会进入待审核队列。
- 增加主题相关度、教材关联度和正文质量阈值；低于阈值的结果计入“过滤数量”，不再增加管理员审核工作量。
- 增加教学重要度评分和重要/重点/观察等级，候选材料默认按教学重要度排序。
- 默认来源巡检周期调整为每日一次，并在来源和任务界面显示最近抓取时间、任务类型、来源范围和处理步骤。
- 发现任务增加“读取来源—提取正文—相关性过滤—待审核”步骤提示及发现、正文、过滤、去重、失败统计；部分失败不再直接显示原始错误文本。
- 新增质量评分、重要度和过滤统计迁移 `20260804_01_discovery_quality_scores.py`。
